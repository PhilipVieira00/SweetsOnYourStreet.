const FormButton = (props) => {
    console.log(props.style);
    return(
        <button></button>
        /*<button 
            style= {props.style} 
            onClick= {props.action}>    
            {props.title} 
        </button>*/)
}

export default FormButton;